from django.apps import AppConfig

class FinancieraConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.financiera"
    verbose_name = "Financiera (stub provisório)"
