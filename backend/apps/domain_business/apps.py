from django.apps import AppConfig

class DomainBusinessConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.domain_business'
    verbose_name = 'Domain Business Logic (Consolidated)'
