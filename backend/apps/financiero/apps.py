from django.apps import AppConfig

class FinancieroConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.financiero"
    verbose_name = "Financiero (stub provisório)"
