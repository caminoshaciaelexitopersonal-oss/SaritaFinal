class NovelAlternativeGenerator:
    """Generates entirely novel alternatives using cross-domain recombination."""
    def generate_novel_alternatives(self, known_space):
        # Generates alternatives outside the standard search space
        return [{"id": "NOVEL-ALT-001", "source": "latent_probe"}]
