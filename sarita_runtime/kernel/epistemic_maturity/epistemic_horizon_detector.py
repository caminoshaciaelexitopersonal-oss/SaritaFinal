class EpistemicHorizonDetector:
    """Detects the current horizon of reliable architectural prediction."""
    def detect_epistemic_horizon(self, state):
        return {"depth": 1000, "unit": "generations"}
